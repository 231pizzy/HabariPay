// src/controllers/payoutController.js
const { Payout, Transaction } = require('../models');

const createPayout = async (req, res) => {
  // Your logic to create payouts
};

module.exports = {
  createPayout,
};
